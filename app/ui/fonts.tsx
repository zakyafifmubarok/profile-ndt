import { Inter, Lusitana, Poppins } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const poppins = Poppins({ subsets: ['latin-ext'], weight: '500'});
export const lusitana = Lusitana({ subsets: ['latin'], weight: '700' });